{% extends "page.njk" %}
{% from "govuk/components/summary-list/macro.njk" import govukSummaryList %}

{% block content %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <div class="govuk-notification-banner" role="region"
        aria-labelledby="govuk-notification-banner-title"
        data-module="govuk-notification-banner">
        <div class="govuk-notification-banner__header">
          <h2 class="govuk-notification-banner__title" id="govuk-notification-banner-title">
            Comments
          </h2>
        </div>
        <div class="govuk-notification-banner__content">
          <p class="govuk-notification-banner__heading">
            From {{data.assetPublisher}}
          </p>
          <p>{{data.decisionNotes}}</p>
        </div>
      </div>
      <span class="govuk-caption-l">Request ID: {{data.requestId}}</span> </div>
    <div class="govuk-grid-column-full" id="checkYourAnswers">
      {% for section in sharedata %}
        <h2 class="govuk-heading-m check-heading">{{section.name}}</h2>
        {{ govukSummaryList({
          rows: section.rows,
          classes: "govuk-!-margin-bottom-9"
          }) 
        }}
      {% endfor %}
    </div>
  </div>
{% endblock %}
